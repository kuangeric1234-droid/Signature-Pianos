# Claude Code metadata. The SEO engine skill is the exception: the cloud
# routine runs it from a fresh checkout, so it has to be in the repo.
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/seo-engine/

# SEO engine scratch (drafts before they're saved to the admin)
tools/seo/drafts/

# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Logs / runtime
*.log
npm-debug.log*
node_modules/

# Vercel build output / link
.vercel/

# Local env (never commit secrets — Vercel manages env vars via its dashboard)
.env
.env.local
.env.production
.env.*.local

# Python bytecode
__pycache__/

# Buyer's guide review renders (rebuild with brand/Communication assets/Buyer guide/src/build.py)
/brand/Communication assets/Buyer guide/src/previews/
